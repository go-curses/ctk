package ctk

// manages selection of text
// - could be combined with xcut (if local)
// - owned by the widget containing the text selected
// - maps to the widget's render buffer
// - doesn't actually store text itself
// - overlay styles when selected?
